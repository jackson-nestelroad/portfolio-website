// This class simply restores the width and color for a particle's stroke.

export default class Stroke {
    constructor(width, color) {
        this.width = width;
        this.color = color;
    }
}